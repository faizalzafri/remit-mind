spring:
  application:
    name: remit-mind
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-3.5-flash
            temperature: 0.3
        embedding:
          api-key: ${GEMINI_API_KEY}
          text:
            model: gemini-embedding-001
